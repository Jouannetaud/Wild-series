{% extends 'base.html.twig' %}

{% block title %}Program
{% endblock %}

{% block body %}
	<h1>Program</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ program.id }}</td>
			</tr>
			<tr>
				<th>Title</th>
				<td>{{ program.title }}</td>
			</tr>
			<tr>
				<th>Summary</th>
				<td>{{ program.summary }}</td>
			</tr>
			<tr>
				<th>Poster</th>
				<td>
					<imgSeasons</th src="{{ program.poster }}" width="300" </td> </tr> <tr> <th>>
						<td>
							<ul>
								{% for season in program.seasons %}

									<li>
										<a href="{{path('program_season_show',{'program': season.program.id, 'season': season.id })}}">Saison
											{{ season.number }}</a>
									</li>


								{% endfor %}

							</ul>

							<strong>Acteurs :</strong>
						</td>
                        <td>	
                            <ul>
								{% for actor in program.actors %}
									<li>
										<a href="{{ path("actor_show", { id: actor.id }) }}">{{ actor.firstname }}</a>
									</li>
								{% else %}
									<li>Aucun acteur pour cette série</li>
								{% endfor %}
							</ul>


						</td>
					</tr>
				</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('program_index') }}">back to list</a>

	<a href="{{ path('program_edit', {'program': program.id}) }}">edit</a>

	{{ include('program/_delete_form.html.twig') }}
{% endblock %}
